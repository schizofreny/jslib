{
  "name": "s9y",
  "version": "0.1.0",
  "packageManager": "pnpm@7.14.1",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "fix": "turbo run fix"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.1"
  }
}
